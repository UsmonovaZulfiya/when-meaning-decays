# Basic evaluation set

Primary evaluation code for:

- exact word-recovery accuracy;
- word-to-guess semantic similarity;
- description-to-initial-description semantic similarity;
- Shannon and normalized entropy.

`run_model_comparison_analysis.py` is the main evaluator. The remaining files
produce compact tables, robustness summaries, figures, and word-level views.
